# AI.md — راهنمای مدل‌های هوش مصنوعی برای کار روی این کدبیس

این فایل، «قوانین سخت‌گیرانه» و زمینهٔ معماری را برای هر مدل AI (یا agent) که به این ریپازیتوری متصل می‌شود مشخص می‌کند. هدف: جلوگیری از تغییرات مخرب و حفظ یکدستی کد.

> اگر یک فایل `.cursorrules` یا `.github/copilot-instructions.md` هم در نصب شما وجود داشته باشد، محتوای این فایل را در آن‌ها کپی/ارجاع دهید.

---

## ۱. حقیقت کجاست (Sources of Truth)

- **`README.md`** — هدف، نصب، پیکربندی، تست.
- **`ARCHITECTURE.md`** — معماری، دیاگرام‌ها، ساختار داده، قرارداد AJAX.
- **`TECH_DEBT.md`** — چیزهایی که باید «نادیده گرفته نشوند» یا «اصلاح شوند».
- **`TODO.md`** — کارهای باز و باگ‌ها.
- خود کد در مسیرهای زیر: قالب‌های PHP ریشه + `panel/`، منطق در `inc/`، asset ها در `assets/`.

قبل از هر پاسخ/تغییر، این اسناد و فایل‌های درگیر را بخوانید؛ **حدس نزنید**.

## ۲. قوانین سخت‌گیرانه (Hard Rules)

### ۲.۱ هرگز (Never)
1. **هرگز URL دامنه یا آدرس asset را هاردکد نکنید.**
   - ممنوع: `https://edu.falnic.com/...`، `/wp-content/themes/edu-falnic/...`
   - مجاز: `home_url()`, `site_url()`, `get_template_directory_uri()`, `PATH_DIR_URL`.
2. **هرگز ورودی کاربر را بدون sanitize و خروجی را بدون escape چاپ نکنید.**
   - به‌ویژه: `$_GET['redirect_to']` و هر مقدار سشن/کوکی.
3. **هرگز short open tag ننویسید** (`<?`, `<?=`) — فقط `<?php ... ?>`.
4. **هرگز اکشن AJAX بدون nonce و بدون sanitize نسازید.**
5. **هرگز مقدار جدیدی را بدون ثبت در مستندات اضافه نکنید** (متا/اکشن/صفحه/تمپلیت/فیلد) — حداقل `CHANGELOG.md` + جدول مربوطه در `ARCHITECTURE.md`.
6. **هرگز `index.php` دیباگ را در تولید نگه ندارید** و هرگز قالب جدیدی را جایگزین آن نکنید بدون بازبینی همهٔ fallback ها.
7. **هرگز اعتبارنامه/کلید (SMS، API،…) را داخل کد نگذارید**؛ به ثابت wp-config/option ارجاع دهید.
8. **هرگز به‌صورت خودسرانه** نام تمپلیت‌ها (`Template Name: Panel - …`)، اسلاگ برگه‌ها، یا کلیدهای متا را عوض نکنید — محتوای دیتابیس لایو به آن‌ها وابسته است.

### ۲.۲ همیشه (Always)
1. سمت‌وسر: `esc_html/esc_url/esc_attr` برای خروجی، `sanitize_*` برای ورودی، `$wpdb->prepare` برای SQL.
2. حفظ RTL؛ متن جدید فارسی؛ استایل با متغیرهای `:root` قالب.
3. برای تکمیل درس/پیشرفت فقط از API های عمومی لرن‌دش.
4. بعد از تغییر: `CHANGELOG.md` به‌روز شود (در صورت لزوم `ARCHITECTURE.md`/`TECH_DEBT.md`).
5. در پایان، با `git diff` خروجی را بازبینی کنید و از نبودن تغییرات ناخواسته مطمئن شوید.

## ۳. زمینهٔ معماری که باید رعایت شود

- پشته: PHP 7.4+/8، وردپرس + LearnDash؛ فرانت: jQuery/Owl/Plyr بدون باندلر.
- صفحات پنل = برگه‌ها با `Template Name` در زیرپوشهٔ `panel/`؛ مسیر واقعی برگه‌ها از اسلاگ آن‌ها می‌آید (نه مسیر فایل).
- احراز هویت: OTP با سشن (`fl_otp*`)؛ ارسال از طریق `inc/sms.php` + تابع بیرونی `falnic_send_otp_with_bale` — هر دو سرویس بیرونی‌اند؛ در لوکال باید mock شوند.
- جدول `{wp}_falnic_transactions` خارج از قالب ساخته می‌شود؛ قالب فقط `SELECT` می‌کند.
- دسترسی درس: `sfwd_lms_has_access()` یا `sample_lesson === 'on'`.
- سبک خطا: پیام‌های فارسی، کاربرپسند؛ خطاهای AJAX با `wp_send_json_error(['message'=>...])`.

## ۴. Anti-pattern های موجود (که تغییر جدید نباید آن‌ها را تکرار کند)

| ضدالگو | محل نمونه | جایگزین |
|---|---|---|
| هاردکد دامنه | `header.php`, `footer.php`, `inc/meta_functions.php`, `panel/*`, `page-login.php`, `front-page.php`, `single-sfwd-courses.php`, `style.css` | توابع وردپرس |
| چاپ خام `$_GET` در inline JS | `page-login.php` (redirect_to) | `esc_url_raw` + `esc_js` |
| HTML با رشتهٔ PHP برای مودال/کارت | `single-sfwd-courses.php` (`$modals_html`) | قالب/partial با escape |
| کد مردهٔ نیمه‌فعال | `inc/captcha.php`, `falnic_submit_cta`, `if(false)` وبینار، PhotoSwipe, `Untitled-1.json` | حذف یا اتصال کامل |
| توابع کمکی تکراری در چند فایل | helpers قیمت/تصویر در front-page و panel | `inc/helpers.php` متمرکز |
| session بدون نام/طول عمر | `inc/login.php` | تنظیمات امن سشن یا جایگزین |

## ۵. روش پاسخ‌دهی مدل (Behavior)

- وقتی از شما دربارهٔ رفتار صفحه/کد سؤال می‌شود: ابتدا فایل قالبِ آن URL (جدول `ARCHITECTURE.md` §5) و `inc` مرتبط را باز کنید، بعد پاسخ دهید و **منبع (فایل:سطر تقریبی)** را ذکر کنید.
- وقتی تغییر می‌خواهند: کوچک‌ترین تغییر امن را پیشنهاد/اعمال کنید؛ اگر چند راه دارد، گزینهٔ سازگار با معماری فعلی را بدهید و Trade-off را توضیح دهید.
- اگر تغییری با «قوانین بخش ۲» در تضاد است: **تغییر را اعمال نکنید**؛ توضیح دهید چرا و پیشنهاد اصلاح‌شده بدهید.
- هرگز از «سبک بازنویسی کلی» (مثلاً ریفرمت کامل یک فایل ۱۰۰۰ خطی یا تغییر نام دسته‌جمعی) بدون درخواست صریح استفاده نکنید.

## ۶. چک‌لیست پایان تغییر (Definition of Done)

- [ ] PHP سالم: `php -l` پاس.
- [ ] بدون هاردکد/بدون short tag/بدون چاپ خام.
- [ ] escape/sanitize رعایت شده؛ اکشن AJAX جدید nonce دارد.
- [ ] RTL و فونت حفظ شده.
- [ ] `CHANGELOG.md` به‌روز شد.
- [ ] ساختار داده/معماری تغییر کرد → `ARCHITECTURE.md` و `TECH_DEBT.md` اصلاح شدند.
- [ ] `git status`/`git diff` بازبینی شد؛ فقط فایل‌های مرتبط تغییر کرده‌اند.
