# CHANGELOG

تمامی تغییرات مهم این پروژه در این فایل ثبت می‌شود.

قالب این فایل از استاندارد **[Keep a Changelog](https://keepachangelog.com/fa-IR/1.1.0/)** پیروی می‌کند و نسخه‌گذاری پروژه از **[Semantic Versioning](https://semver.org/)** (به شکل `<major>.<minor>.<patch>`) تبعیت می‌کند.

## انواع تغییرات (طبق استاندارد)

- **`Added`** — قابلیت‌های جدید
- **`Changed`** — تغییر در قابلیت‌های موجود
- **`Deprecated`** — قابلیت‌هایی که به‌زودی حذف می‌شوند
- **`Removed`** — قابلیت‌های حذف‌شده در این نسخه
- **`Fixed`** — رفع باگ
- **`Security`** — رفع آسیب‌پذیری

---

## [Unreleased]

### Added

- **جایگزین «نشانی تصویر» در مدیریت اسلایدرها**: هر ردیف یک فیلد `slides[…][image_url]` دارد که هم پیش‌نمایش را به‌روز می‌کند و هم در نبودِ کتابخانهٔ رسانه (`wp.media`) راه جایگزین ذخیرهٔ اسلاید است؛ دکمهٔ «پاک کردن» تصویر، پیام‌های درون‌ردیفی (انتخاب/پاک‌کردن/خطا) و یک watchdog درون‌صفحه‌ای که اگر اسکریپت اسلایدرها بارگذاری نشود به مدیر هشدار می‌دهد هم اضافه شد.
- **تست خودکار صفحهٔ تنظیمات قالب**: `test-admin-settings.cjs` (۵۶ بررسی با jsdom روی HTML رندرشدهٔ واقعی + فایل JS واقعی قالب: باز شدن کتابخانهٔ رسانه، نشستن پیوست در ردیف، frame جدا برای هر ردیف، افزودن/حذف/پاک‌کردن، ورود دستی نشانی و نبود `wp.media`)، `test-admin-watchdog.cjs` (۸ بررسی) و `test-pages.cjs` (۱۵۸ بررسی ظاهری روی ۲۰ صفحه با CSS واقعی قالب).
- **سکشن «بلاگ ویژه» در صفحهٔ اصلی**: کارت بزرگ نوشتهٔ شاخص (تصویر، دسته، خلاصه، نویسنده، تاریخ شمسی، دکمهٔ مطالعه) به‌همراه سه ردیف نوشتهٔ کنار آن؛ داده از `evented_featured_posts()` (نوشته‌های چسبان وردپرس و در ادامه آخرین نوشته‌ها، با فیلتر `evented_featured_posts`) و در نبود نوشته کل بخش چاپ نمی‌شود. استایل در `evented-home.css`.
- **استایل کامل صفحهٔ آزمون در `ee-lms.css`**: کارت آزمون، گرید فکت‌ها (۲/۳/۵ ستونه واکنش‌گرا)، اعلان ورود، و پوستهٔ خروجی موتور آزمون لرن‌دش — بلاک سوال، گزینه‌ها، نتیجهٔ درست/نادرست، دکمه‌ها، ناوبری، نوار پیشرفت/تایمر، جدول نتایج، سوالات مرتب‌سازی/دسته‌بندی و گواهینامه (هر دو خانوادهٔ کلاس `wpProQuiz_*` و `ld-quiz-*`).
- **بررسی خودکار پوشش CSS** (`css_coverage.py` در بنچ تست): هر عنصر `ee-*` در همهٔ قالب‌ها/پارت‌ها باید دست‌کم یک قاعدهٔ CSS داشته باشد؛ همین بررسی کلاس‌های بی‌استایل صفحهٔ آزمون، وبینار و بلوک پیشرفت را پیدا کرد.
- **قالب صفحهٔ آزمون (`single-sfwd-quizzes.php`)**: عنوان + نوار اطلاعات (تاریخ شمسی، ساعت، بازدید) + نوار وضعیت/مسیریابی «خانه › دوره › آزمون»، کارت فکت‌های آزمون (تعداد سوال، محدودیت زمانی، تعداد دفعات مجاز، درصد قبولی، گواهینامه)، بدنهٔ آزمون از شورت‌کد لرن‌دش (`[ld_quiz]` و در صورت نبود آن `[learndash_quiz]` و در نهایت `the_content()`)، اعلان ورود برای کاربر مهمان، دکمهٔ بازگشت به دوره و سایدبار (فهرست درس‌های دوره + کارت ثبت‌نام + سایر دوره‌ها).
- **سایر صفحات با پوستهٔ جدید**: `archive-sfwd-courses.php` (بایگانی همهٔ دوره‌ها — قبلاً فقط یک shim به taxonomy بود)، `taxonomy-ld_course_category.php` (سربرگ دسته با تصویر/توضیح کوتاه، چیپ زیردسته‌ها، گرید دوره‌ها، صفحه‌بندی، سوالات متداول دسته)، `page-courses.php` (برگهٔ دوره‌ها — قبلاً فقط عنوان‌ها را چاپ می‌کرد)، `page-courses-cat.php` (گرید دسته‌بندی‌ها)، `author.php` (پروفایل مدرس: آواتار، بیو، شبکه‌های اجتماعی، آمار دوره/دانشجو/امتیاز، دوره‌ها و مقالات)، `template-instructors.php` (فهرست اساتید `group_leader` با صفحه‌بندی)، `page.php` (برگهٔ عمومی: کارت + سایدبار ویجت‌ها)، `index.php` (fallback: تک‌محتوا یا بدنهٔ مشترک آرشیو) و `404.php` (پیام، جستجو، لینک‌ها و پیشنهاد دوره).
- **پارت‌های مشترک LMS**: `template-parts/lms/course-grid.php` (کارت دوره: تصویر، دسته، قیمت، تعداد جلسه، مدت، سطح، مدرس، وضعیت ثبت‌نام)، `template-parts/lms/category-grid.php` (کارت دسته با تصویر `ld_cat_image_id` و شمار دوره‌ها) و `template-parts/lms/courses-sidebar.php` (جستجو، دسته‌ها با علامت دستهٔ جاری، آخرین دوره‌ها، اشتراک‌گذاری، کانال‌ها).
- **استایل فهرست‌ها**: `assets/css/newhome/ee-courses.css` (گرید دوره/دسته/اساتید، سربرگ فهرست، چیپ‌ها، صفحه‌بندی، سوالات متداول، پروفایل مدرس، کارت برگه، ۴۰۴، چاپ و `prefers-reduced-motion`).
- **هلپرهای جدید در `inc/template_helpers.php`**: `evented_logo_html()`، `evented_course_card_data()`، `evented_courses_query()`، `evented_pagination()`، `evented_term_image()`، `evented_instructor_data()`، `evented_instructors()`، `evented_quiz_data()` و `evented_is_standalone_page()`.

### Changed

- **بازنویسی `assets/js/admin/theme-settings.js`**: همهٔ رویدادها از `document` واگذار می‌شوند (نه وابسته به زمان اجرای اسکریپت)، برای هر ردیف یک frame رسانه ساخته و در `data` همان ردیف نگه داشته می‌شود، متن‌ها با `wp_localize_script` (شیء `eeSettings`) تزریق می‌شوند و هر ردیف `data-uid` یکتا می‌گیرد.
- **تشخیص صفحهٔ تنظیمات قالب**: `evented_is_theme_settings_screen()` علاوه بر هوک `appearance_page_evented-theme-settings`، slug صفحه (`$_GET['page']`) را هم بررسی می‌کند تا با تغییر منو/هوک، کتابخانهٔ رسانه از کار نیفتد.
- **حذف متن لورم ایپسوم از حالت‌های خالی پنل** (داشبورد، پرداخت‌ها، علاقه‌مندی، گواهینامه‌ها) با متن واقعی فارسی، و پیوند «مشاهده دوره‌ها» از `/` به آرشیو دوره‌ها (`get_post_type_archive_link('sfwd-courses')`).
- **هم‌راستاسازی راهنمای صفحهٔ اسلایدرها با رفتار واقعی**: پیش‌تر نوشته بود «در نبود اسلاید، بنر پیش‌فرض نمایش داده می‌شود» در حالی که هیرو عمداً بدون اسلاید رندر می‌شود (`.ee-hero.is-no-slider`).
- **بازبینی فاصله‌های هدر**: فاصلهٔ افقی ردیف هدر از `.75rem` به `1.5rem`، فاصلهٔ عمودی `padding-block: .8rem` روی `.ee-header > .ee-wrap` (قاعدهٔ قبلی `.ee-header-in` مرده بود چون آن کلاس در مارک‌آپ وجود ندارد)، فاصلهٔ آیتم‌های منو از `.2rem` به `.55rem` با padding بزرگ‌تر، جستجو با `margin-inline: auto` میان ردیف مرکزچین شد، و کلاس‌های مردهٔ Tailwind (`text-slate-500 font-medium`) با `ee-tb-label` جایگزین شدند.
- **بازبینی صفحهٔ تک‌درس**: ستون محتوا کارت سفید شد (`.ee-lesson-main` هم‌خانوادهٔ کارت دوره)، سایدبار از `23rem` به `21rem` (ستون محتوا پهن‌تر) با فاصلهٔ `1.5rem`، ظرف صفحه روی نمایشگرهای بزرگ‌تر `100rem`، عنوان/نوار اطلاعات/مسیریابی بزرگ‌تر و فاصلهٔ بخش‌ها (آکاردئون‌ها، آزمون، تکمیل، ناوبری) بازبینی شد.
- **ظرف‌های بی‌استایل**: `.ee-webinar`، `.ee-progress`، `.ee-wishlist-text`، `.ee-404-more` و `.ee-home-main` قاعدهٔ CSS گرفتند.
- **لینک‌های سایدبار پنل**: شش لینک سخت‌کد `/panel/…` به `home_url()` تبدیل شدند (سازگار با نصب در زیردامنه/زیرپوشه).
- **لوگوی هدر و فوتر از تنظیمات وردپرس**: تصویر ثابت `evented-edu-logo.webp` حذف شد؛ `evented_logo_html()` از `get_theme_mod('custom_logo')` + `wp_get_attachment_image()` استفاده می‌کند (پشتیبانی `custom-logo` در `inc/theme_options.php` ثبت شد) و در نبود لوگو، نام سایت نمایش داده می‌شود. فیلتر `evented_logo_html` برای بازنویسی مارک‌آپ وجود دارد.
- **اسلایدر هیرو فقط از تنظیمات قالب**: شاخهٔ `else` در `front-page.php` (ساخت اسلاید از «آخرین دورهٔ ویژه» یا تصویر پیش‌فرض) حذف شد؛ اگر در تنظیمات اسلایدی نباشد هیرو تک‌ستونی و بدون اسلایدر رندر می‌شود (`.ee-hero.is-no-slider`) و یک کوئری اضافهٔ `WP_Query` هم از صفحهٔ اصلی حذف شد.
- **پوشش `evented_is_ee_view()`**: حالا همهٔ نماهای عمومی (آزمون، بایگانی/دستهٔ دوره، برگه‌ها، اساتید، ۴۰۴) پوستهٔ جدید را می‌گیرند؛ تنها استثنا برگه‌های مستقل هستند (`page-login.php`، `page-panel.php` و قالب‌های `panel/*`) که با `evented_is_standalone_page()` تشخیص داده می‌شوند.
- **Enqueue**: شاخهٔ LMS شامل `sfwd-quizzes` شد و برای نمای فهرست‌ها `ee-courses.css` بارگذاری می‌شود. شاخه‌های مردهٔ `assets/assets_functions.php` حذف شدند (`archive-courses.css`، `author.css`/`author.js`، `single-page.css` و `archive-product.css` — دو فایل آخر اصلاً وجود نداشتند و ۴۰۴ می‌شدند).

### Fixed

- **کار نکردن دکمهٔ «انتخاب تصویر» در تنظیمات قالب** — دو مسیر شکست بی‌صدا در JS وجود داشت و هر دو حذف شدند: (۱) اسکریپت بلافاصله هنگام اجرا `$('#evented-slides-list')`/`$('#evented-slide-proto')` را می‌گرفت و در نبودشان با `return` خاموش بیرون می‌رفت (حالا رویدادها از `document` واگذار می‌شوند)؛ (۲) در نبود `wp.media` (بارگذاری‌نشدن اسکریپت‌های رسانه به هر دلیل) کلیک با `ReferenceError` بی‌پاسخ می‌ماند (حالا پیام خطا چاپ و فیلد «نشانی تصویر» فوکوس می‌شود). مسیر جایگزین، ذخیره‌سازی و رندر فرانت‌اند هم برای اسلاید بدون پیوست کامل شد.
- **«افزودن اسلایدر» ردیف ناقص می‌ساخت**: فقط `innerHTML` قالب کلون کپی می‌شد، بنابراین ردیف جدید پوستهٔ `.evented-slide-row` (کارت، حاشیه، فاصله) را نداشت و `data-uid` روی `.evented-slide-main` می‌نشست؛ حالا با `clone()` کل ردیف کپی و `__UID__` در `name` فیلدها جایگزین می‌شود.
- **لوگوی سفارشی وردپرس در فوتر بدون استایل بود**: `.ee-f-logo` فقط `.logo-fallback` را استایل می‌کرد و `<img class="ee-logo-img">` با اندازهٔ طبیعی رندر می‌شد؛ قواعد `.ee-logo .ee-logo-img` (۴۴px) و `.ee-f-logo .ee-logo-img` (۴۰px) با `object-fit: contain` اضافه شد.
- **کلاس‌های بی‌استایل پنل**: `.btn-primary` (دکمهٔ حالت‌های خالی)، `.course-actions` (ردیف دکمه‌های کارت دوره در «دوره‌های من» که فلکس نبود) و `.password-rules li.invalid` (حالت نامعتبر قواعد رمز عبور) استایل گرفتند؛ بررسی پوشش CSS حالا روی همهٔ ۲۶ صفحهٔ رندرشده صفر عنصر بی‌استایل گزارش می‌کند.
- **`evented_get_home_slides()` اسلاید دارای نشانی ولی بدون شناسهٔ پیوست را دور می‌انداخت** (شرط `!$id || !$img`)؛ حالا شرط فقط نبودِ نشانی است و در صورت نیاز نشانی از پیوست بازخوانی می‌شود.
- **بازنشدن صفحهٔ پنل کاربری (`/panel`)** — سه باگ مستقل:
  - `panel/dashboard.php` از `$wpdb` بدون `global $wpdb;` استفاده می‌کرد؛ در scope قالب مقدارش `null` است و روی PHP 8 خطای مرگبار `Call to a member function get_var() on null` می‌داد (با اجرای واقعی قالب در بنچ تست بازتولید و رفع شد).
  - هر هفت قالب پنل سایدبار را با مسیر نسبی (`include_once 'sidebar.php'`) صدا می‌زدند؛ چون CWD پوشهٔ قالب نیست، سایدبار اصلاً include نمی‌شد. به `locate_template('panel/sidebar.php', true, false)` تغییر کرد.
  - `panel.css`/`panel.js` فقط برای برگهٔ `panel` و زیربرگه‌هایش بارگذاری می‌شد؛ برگه‌هایی که یکی از قالب‌های `panel/*.php` (Template Name) رویشان انتخاب شده ولی زیر `panel` نیستند بی‌استایل باز می‌شدند. شرط enqueue با `get_page_template_slug()` گسترش یافت.
- **خطای مرگبار PHP 8 در `evented_adjacent_steps()`**: `isset($step['post'])` روی شیء `WP_Post` (خروجی ممکن `learndash_get_course_steps()`) باعث `Cannot use object of type WP_Post as array` می‌شد؛ دسترسی به شکل type-safe بازنویسی شد (این خطا با اجرای واقعی قالب‌ها روی PHP 8.3 کشف و رفع شد).

### Added (پیشین — تک‌درس و تک‌دوره)

- **قالب تک‌درس (`single-sfwd-lessons.php`) بر پایهٔ الگوی صفحهٔ درس «شمیم»**: عنوان درس، نوار اطلاعات (تاریخ شمسی، ساعت، دیدگاه، بازدید، مدت)، نوار وضعیت + مسیریابی «دوره › درس»، آکاردئون‌های **کلیپ/پادکست/متن** (با فایل‌های پیوست)، دکمهٔ بزرگ **آزمون**، دکمهٔ «علامت‌گذاری به‌عنوان تکمیل‌شده» (AJAX + به‌روزرسانی درجا) و ناوبری قبلی/بعدی. درس قفل‌شده به‌جای رسانه جعبهٔ ثبت‌نام نشان می‌دهد.
  - `template-parts/lms/lesson-list.php` — سایدبار فهرست درس‌ها: نوار پیشرفت، گروه‌بندی بر اساس فصل با دکمهٔ «باز کردن همه»، نشانهٔ وضعیت هر درس (تکمیل/جاری/قفل/پیش‌نمایش)، مدت، تعداد آزمون و لینک بازگشت به دوره.
  - `inc/template_helpers.php` — `evented_lesson_media()` (ویدیو/پوستر/پادکست/پیوست)، `evented_lesson_quizzes()`، `evented_adjacent_steps()` و `evented_step_state()`.
- **استایل و رفتارهای LMS**: `assets/css/newhome/ee-lms.css` (کارت دوره، سرفصل‌ها، آکاردئون، پخش‌کننده، فهرست درس‌ها، کارت ثبت‌نام، نوار خرید موبایل، `prefers-reduced-motion` و حالت چاپ) + `assets/js/newhome/ee-lms.js` (بدون jQuery: آکاردئون، گروه‌های جمع‌شونده، امتیاز ستاره‌ای، دیدگاه، تکمیل درس، علاقه‌مندی، اسکرول به درس جاری).

- **قالب تک‌نوشته (`single.php`) بر پایهٔ الگوی صفحهٔ تک‌مقالهٔ «شمیم»**: کارت سفید مقاله در ستون راست (۸ ستون) و سایدبار ویجت‌ها در ستون چپ (۴ ستون) — مسیریابی، عنوان مرکزی، تصویر شاخص، نوار اطلاعات (دسته، تاریخ شمسی، ساعت، زمان مطالعه، تعداد دیدگاه، تعداد بازدید)، نوار نویسنده، تایپوگرافی کامل متن (justify با `line-height: 2.15`، blockquote، جدول، کد)، برچسب‌ها، اشتراک‌گذاری، ناوبری نوشتهٔ پیشین/پسین، جعبهٔ دربارهٔ نویسنده، نوشته‌های مرتبط و دیدگاه‌ها.
  - `single.php`، `comments.php` (لیست دیدگاه + `comment_form()` با لیبل فارسی).
  - `template-parts/ee-sidebar.php` — ویجت‌های جستجو، اشتراک‌گذاری، آخرین مطالب، ویژه‌ها، مطالب پربازدید، دوره‌ها و «ما را دنبال کنید».
  - `inc/template_helpers.php` — هلپرها: تبدیل شمسی (`evented_gregorian_to_jalali`/`evented_format_jalali`، فقط وقتی افزونهٔ شمسی‌ساز فعال نباشد)، زمان مطالعه، شمارندهٔ بازدید (`evented_post_views`)، لینک‌های اشتراک/کانال (فیلترپذیر با `evented_share_links` و `evented_channel_links`) و نوشته‌های مرتبط.
- **آرشیو نوشته‌ها**: `archive.php` (دسته/برچسب/بایگانی زمانی)، `home.php` (برگهٔ نوشته‌ها) و `search.php` (نتایج جستجو) که هر سه از یک بدنهٔ مشترک استفاده می‌کنند:
  - `template-parts/ee-archive-main.php` — سربرگ بایگانی (عنوان، توضیح، تعداد نوشته)، چیپ‌های فیلتر دسته‌بندی با شمارنده، شبکهٔ کارت نوشته‌ها (تصویر، دسته، چکیده، تاریخ، زمان مطالعه، بازدید، دیدگاه) و صفحه‌بندی `paginate_links` + حالت «موردی یافت نشد» با فرم جستجو.
- **پوستهٔ مشترک `ee-*`**: `template-parts/ee-head.php`، `ee-header.php` و `ee-footer.php` از `front-page.php` استخراج شدند تا صفحهٔ اصلی، تک‌نوشته و آرشیو هدر/فوتر واحد داشته باشند (خروجی صفحهٔ اصلی بدون تغییر؛ فقط لینک‌های `#` به مقصدهای واقعی و آیتم فعال منو داینامیک شد).
  - `assets/css/newhome/ee-shell.css` — توکن‌های طراحی، ریست، هدر/ناوبری، فوتر، نوار موبایل و ویجت‌های سایدبار (از `evented-home.css` جدا شد؛ بدون هیچ سلکتور مشترک).
  - `assets/css/single-post.css` و `assets/css/archive-post.css` (که پیش‌تر خالی/ناموجود بودند و ۴۰۴ می‌دادند).

### Changed

- **بازطراحی کامل صفحهٔ دوره (`single-sfwd-courses.php`)** بر پایهٔ الگوی صفحهٔ دورهٔ «شمیم» و با پوستهٔ مشترک `ee-*`: کارت سفید دوره (مسیریابی، عنوان/زیرعنوان، بنر، نوار اطلاعات، نوار وضعیت پیشرفت)، سرفصل‌های گروه‌بندی‌شده بر اساس فصل، آکاردئون‌های معرفی/دستاوردها/سوالات متداول/وبینار، جعبهٔ مدرس، بخش نظرات با امتیاز ستاره‌ای و سایدبار (ثبت‌نام، جستجو، اشتراک‌گذاری، سایر دوره‌ها، آخرین دوره‌ها، دنبال‌کردن).
  - **پرفورمنس**: حذف `wp_oembed_get()` و مودال ویدیو برای هر درس (N درخواست شبکه و N مودال در DOM)، حذف Plyr، کش دادهٔ گام‌ها در `evented_course_steps()` و خواندن یک‌جای درس‌های تکمیل‌شده؛ رسانهٔ درس فقط در صفحهٔ همان درس و یک‌بار ساخته می‌شود.
  - قطعه‌های جدید: `template-parts/lms/curriculum.php`، `enroll-card.php`، `course-sidebar.php`.
- **Enqueue**: `evented_is_ee_view()` اکنون `is_singular('sfwd-courses'|'sfwd-lessons')` را هم پوشش می‌دهد و `functions.php` فایل‌های `ee-lms.css/js` را با لوکال‌سازی `eeLms.ajax_url` بارگذاری می‌کند؛ شاخهٔ قدیمی Plyr + `single-courses.css/js` در `assets/assets_functions.php` خالی شد.
- **Enqueue**: بارگذاری پوستهٔ جدید از `functions.php` با شرط `evented_is_ee_view()` و اولویت ۲۰ انجام می‌شود؛ شاخه‌های `archive-post.*` و `single-post.*` در `assets/assets_functions.php` حذف شدند (فایل‌های JS آن‌ها وجود نداشت) و `is_home()` از شاخهٔ `front-page.css` بیرون آمد.
- **JS صفحهٔ اصلی** (`assets/js/newhome/evented-home.js`): افزودن کپی لینک اشتراک‌گذاری با `navigator.clipboard` (و مسیر جایگزین `execCommand`) + Toast.

### Added (پیشین)

- **مدیر اسلایدر هیروی صفحهٔ اصلی** (زیرمنوی «تنظیمات قالب» در پیشخوان): افزودن/حذف چند اسلاید با انتخاب تصویر از کتابخانهٔ رسانه (wp.media)، به‌همراه برچسب، عنوان، توضیح و لینک برای هر اسلاید. داده‌ها در آپشن `evented_home_slides` ذخیره می‌شوند؛ بدون اسلاید تنظیم‌شده، هیرو مثل قبل آخرین دورهٔ ویژه را نمایش می‌دهد.
  - `inc/theme_settings.php` — منطق ذخیره/خواندن + صفحهٔ پیشخوان (`evented-theme-settings`).
  - `assets/css/admin/theme-settings.css` و `assets/js/admin/theme-settings.js` — استایل و رفتار ردیف‌های اسلاید در پیشخوان.
  - اسلایدر سمت سایت در `front-page.php` با استایل/رفتار در `assets/css/newhome/evented-home.css` و `assets/js/newhome/evented-home.js` (پیکان‌ها، نقطه‌ها، چرخش خودکار ۶ ثانیه‌ای، توقف با هاور، احترام به `prefers-reduced-motion`).
- افزودن مستندات استاندارد و کامل پروژه به ریشهٔ قالب:
  - `README.md` — نقطهٔ ورود: هدف، پشتهٔ فناوری، ساختار، نصب، پیکربندی، تست‌ها.
  - `ARCHITECTURE.md` — معماری، جریان داده و دیاگرام‌ها (Mermaid)، نقشهٔ داده و قراردادهای AJAX.
  - `CONTRIBUTING.md` — استانداردهای توسعه، قرارداد Conventional Commits، فرایند PR.
  - `AI.md` — قوانین و محدودیت‌های مدل‌های AI هنگام کار روی کدبیس.
  - `ROADMAP.md` — نقشهٔ راه نسخه‌ها.
  - `TODO.md` — تسک‌های معلق و باگ‌های شناخته‌شده.
  - `TECH_DEBT.md` — فهرست بدهی فنی و نقاط نیازمند ریفکتور.
- نگاشت کامل «صفحات ↔ تمپلیت‌ها»، «کلیدهای متا/سشن/جدول سفارشی» و «اکشن‌های AJAX» در مستندات ثبت شد.

### Changed

- جایگزینی مستندات شماره‌دار قبلی (`docs/00-overview-and-index.md` تا `docs/10-known-issues-and-todo.md`، که سپس به ریشهٔ قالب منتقل شده بودند) با مجموعهٔ استاندارد نام‌گذاری‌شده (`README.md`، `ARCHITECTURE.md`، …) مطابق قراردادهای رایج مخازن؛ محتوای فنی حفظ و بازنویسی/بازچینش شد.

### Changed

- **برندینگ «edu falnic»/«فالنیک» → «evented-edu»**: پیشوند شناسه‌ها `falnic_*` → `evented_*`، کلاس `FalnicAuthHandler` → `EventedAuthHandler`، اکشن‌های AJAX و nonce (`falnic_nonce` → `evented_nonce`)، کلیدهای سشن (`fl_otp*`/`fl_mobile` → `evented_otp*`/`evented_mobile`)، نام فایل‌های فونت/لوگو (`evented-edu-font.woff2`، `evented-edu-logo*`)، هدر `style.css` و متن‌های فارسی UI.
- **حذف URL های هاردکد دامنه** (assets و ریدایرکت‌ها) و جایگزینی با `PATH_DIR_URL` / `get_template_directory_uri()` / `home_url()` / `wp_login_url()` — قالب روی هر دامنه/مسیری قابل اجراست.
- ارجاع جدول سفارشی تراکنش `{wp}_falnic_transactions` → `{wp}_evented_transactions` (⚠️ migration — پایین را ببینید).
- `page-panel.php`: کاربرِ لاگین‌شده به `/panel/my-courses` هدایت می‌شود (جای صفحهٔ خالی).
- صفحات پنل: گارد لاگین برای `/panel/profile` اضافه شد؛ ریدایرکت‌های گارد بدون `.php` و از طریق `wp_login_url()` با `redirect_to` شدند؛ صفحهٔ گواهینامه‌ها دیگر به اشتباه به payments نمی‌فرستد.
- `index.php`: از اسکریپت دیباگ به قالب fallback استاندارد (فهرست/بایگانی) تبدیل شد.
- تنظیمات حساب: ایمیلِ placeholder تولیدشدهٔ ثبت‌نام موبایلی دیگر با مقدار جعلی نمایش داده نمی‌شود (فیلد خالی + placeholder واقعی).
- placeholder ایمیل کاربران جدید: `{mobile}@evented-edu.user`.

### Fixed

- **ثبت‌نام نهایی**: لاگین خودکار پس از `wp_set_password()` اصلاح شد (این تابع void برمی‌گرداند؛ حالا از `$user->ID` استفاده می‌شود).
- **OTP**: کد باید ابتدا درخواست شده باشد؛ انقضای ۱۰ دقیقه؛ تطبیق شمارهٔ موبایل با کد؛ سقف ۵ تلاش اشتباه (پس از آن کد باطل می‌شود)؛ `rand()` → `wp_rand()`؛ شمارندهٔ تلاش هنگام ارسال کد جدید ریست می‌شود.
- **ثبت‌نام/نام**: `handle_save_user_register_name` فقط با OTP تأییدشدهٔ همان شماره حساب می‌سازد و آرگومان نامعتبر `full_name` حذف شد.
- حذف urlencode دستیِ اضافه در `redirect_login_url` (double-encode بودن `redirect_to`).
- فراخوانی `learndash_user_get_enrolled_courses()` در نبود LearnDash دیگر Fatal نمی‌سازد (گارد + fallback خالی).
- `author.php`: متغیر `$facebook` تعریف شد؛ منطق ستاره‌های امتیاز در RTL اصلاح شد (`assets/js/author.js`).
- حذف `</div>` اضافه در هیروی `front-page.php`.

### Security

- ورود با رمز: محدودیت ۵ تلاش ناموفق در ۱۵ دقیقه برای هر شماره (transient) — پس از آن تا ۱۵ دقیقه مسدود است.
- جلوگیری از تزریق در inline JS: `redirect_to` با `wp_validate_redirect` + `wp_json_encode` مقداردهی شد (قبلاً `$_GET['redirect_to']` خام چاپ می‌شد).
- ارسال OTP از طریق wrapper محافظت‌شدهٔ `evented_send_otp_with_bale()` — اگر سرویس بله در دسترس نباشد، به‌جای Fatal فقط لاگ می‌شود؛ نام قدیمی `falnic_send_otp_with_bale()` هم به‌عنوان fallback پشتیبانی می‌شود.

### Removed

- اکشن‌های AJAX بدون هندلر `falnic_submit_cta`/`evented_submit_cta` (متدی وجود نداشت).
- کد مردهٔ `$crm_guids` در `EventedAuthHandler`.
- کامنت اسکریپت particle (کانفیگ مرده) در `front-page.php`.

> ⚠️ **Migration (لازمالاجرا):** اگر در نصب لایو جدول تراکنش‌ها `{wp}_falnic_transactions` است، پیش از استقرار آن را تغییر نام دهید
> (`ALTER TABLE wp_falnic_transactions RENAME TO wp_evented_transactions;`). نشست‌های OTP و nonce های قبلی با نام‌های قدیمی نامعتبر می‌شوند (طراحی عمدیِ برندینگ).

> ℹ️ **یادداشت تاریخچه:** ریپازیتوری عمومی پیش از این مرحله فقط شامل یک کامیت اولیه («a»، کامیت `875fd77`) بوده و نسخه‌ای تگ/منتشر نشده است؛ به همین دلیل بخش `[Unreleased]` نقطهٔ شروع ثبت تغییرات است. نسخهٔ فعلی قالب مطابق هدر `style.css` برابر `1.0.0` است. از این پس **هر تغییر کد** باید یک ورودی در این فایل ایجاد کند (بخش ۵ از `CONTRIBUTING.md`).

---

## قالب استاندارد برای نسخه‌های آینده

```markdown
## [1.1.0] - 2026-09-08

### Added
- توضیح قابلیت جدید.

### Changed
- توضیح تغییر رفتار موجود.

### Deprecated
- توضیح قابلیت در شرف حذف.

### Removed
- توضیح قابلیت حذف‌شده.

### Fixed
- توضیح باگ رفع‌شده.

### Security
- توضیح آسیب‌پذیری رفع‌شده.
```
