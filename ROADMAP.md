# ROADMAP — نقشهٔ راه توسعه

> وضعیت فعلی پروژه: نسخهٔ `1.0.0` (طبق `style.css`) در حال استفاده در `edu.falnic.com`. این نقشهٔ راه بر اساس وضعیت واقعی کد و «بدهی‌های ثبت‌شده» تنظیم شده است. ترتیب اولویت‌ها از «شکستن سایت در آینده» به «بهبود تجربه» است.

## ۱. اهداف کلان (Vision)

1. **پایداری**: حذف وابستگی‌های پنهان (جدول تراکنش، تابع بله، هاردکدها) تا قالب روی هر هاست/دامنه‌ای بدون تغییر کد اجرا شود.
2. **امنیت**: بستن شکاف‌های احراز هویت و خروجی.
3. **یکپارچگی محتوا**: داینامیک‌کردن بخش‌های استاتیک صفحهٔ اصلی و آرشیوها تا تیم محتوا بدون کدنویسی به‌روزرسانی کند.
4. **عملکرد و نگهداری**: کاهش حجم/تکرار کد، حذف asset های مرده، ابزار lint/test.

## ۲. فازبندی (Phases)

### فاز ۱ — «پایداری و امنیت» (P0 — بالاترین اولویت)

اهداف و تسک‌ها (جزئیات هر آیتم در `TODO.md` و `TECH_DEBT.md`):

| اولویت | موضوع | دلیل |
|---|---|---|
| P0 | انتقال هاردکدهای دامنه به توابع وردپرس | بدون آن لوکال/تغییر دامنه می‌شکند |
| P0 | محافظت از فراخوانی `falnic_send_otp_with_bale()` | Fatal Error در تولید هنگام ارسال OTP |
| P0 | اصلاح `save_user_register_name` (الزام تأیید OTP) | ساخت حساب انبوه |
| P0 | escape کردن `redirect_to` در `page-login.php` | XSS/redirect باگ |
| P0 | انتقال اعتبارنامهٔ SMS از سورس به wp-config | افشای راز |
| P1 | نوشتن `index.php` واقعی (حذف باکس دیباگ) | fallback خراب |
| P1 | اصلاح ریدایرکت `panel/certificates.php` و redirect_to های `.php` | مسیر اشتباه کاربر |

### فاز ۲ — «یکپارچگی ویژگی‌ها» (P1)

| اولویت | موضوع |
|---|---|
| P1 | فعال‌سازی واقعی «آپلود تمرین‌ها» در صفحهٔ دوره (فعلاً UI) |
| P1 | تکمیل «ثبت امتیاز به استاد» (فعلاً UI بدون اکشن) |
| P1 | داینامیک‌کردن بخش‌های استاتیک صفحهٔ اصلی (دسته‌ها/اساتید/نظرات/مقالات) |
| P1 | اتصال فیلترهای آرشیو/مرتب‌سازی به کوئری واقعی |
| P1 | رفع «دوره‌های مرتبط» بدون فیلتر دسته (فعال‌سازی tax_query کامنت‌شده) |
| P2 | فعال‌سازی/حذف قطعی captcha (و افزودن فونت TTF) |
| P2 | پروفایل کامل کاربر (آپلود آواتار در پنل، استفاده از نام فارسی در گواهینامه) |

### فاز ۳ — «نگهداری و زیرساخت» (P2/P3)

| اولویت | موضوع |
|---|---|
| P2 | حذف/اصلاح asset های مفقود یا خالی (۴۰۴ها) — لیست در `TODO.md` |
| P2 | استخراج helper های تکراری به `inc/helpers.php` |
| P2 | افزودن CI ساده: `php -l` روی همهٔ فایل‌ها + lint JS (در صورت امکان) |
| P3 | بازنویسی تدریجی `single-sfwd-courses.php` به partial ها |
| P3 | حذف کد مرده (PhotoSwipe، `falnic_submit_cta`، `if(false)` وبینار، `Untitled-1.json`) |
| P3 | تست خودکار جریان OTP (با mock پیامک/بله) |

## ۳. ویژگی‌های برنامه‌ریزی‌شده برای نسخه‌های آینده

| نسخهٔ هدف | ویژگی |
|---|---|
| `1.1.0` | خروجی‌های فاز ۱ (پایداری/امنیت) + `index.php` واقعی |
| `1.2.0` | آپلود تمرین + امتیاز به استاد + فیلترهای آرشیو |
| `1.3.0` | صفحهٔ اصلی کاملاً داینامیک و مدیریت‌شونده |
| `2.0.0` | (چشم‌انداز) ریفکتور بزرگ: قالب‌بندی فایل‌ها، حذف کامل هاردکد و کد مرده، ابزارهای lint/test |

## ۴. خروجی‌های معیار (Definition of Done هر فاز)

- [ ] اجرای لوکال قالب بدون تغییر کد (فقط تنظیم wp-config) ممکن باشد.
- [ ] هیچ آدرس `edu.falnic.com` هاردکد در قالب باقی نمانده باشد.
- [ ] هیچ باگ «بحرانی» باز در `TODO.md` نباشد.
- [ ] `CHANGELOG.md` تاریخچهٔ شفاف از `1.1.0` به بعد داشته باشد.
